export const selectCurrentGameInfo = (state) => state.currentGame.gameInfo;

export const selectCurrentGame = (state) => state.currentGame;

export const selectField = (state) => state.currentGame.field;